# TE\_PARALLEL\_COMPILER

## 功能描述

算子最大并行编译进程数，当该参数大于1时，系统将开启并行编译功能。

网络模型较大时，可通过配置此环境变量开启算子的并行编译功能。

## 配置示例

```bash
export TE_PARALLEL_COMPILER=8
```

## 使用约束

最大不超过CPU核数\*80%/AI处理器个数，取值范围1\~32，默认值是8。

## 产品支持情况

全量芯片支持

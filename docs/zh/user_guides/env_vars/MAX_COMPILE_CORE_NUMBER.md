# MAX\_COMPILE\_CORE\_NUMBER

## 功能描述

此环境变量用于指定图编译时可用的CPU核数。

该环境变量需要配置为整数，表示图编译时开启多线程，多线程数量与配置的CPU核数相同。

取值范围：1\~CPU核数，默认为1，不开启多线程；取值小于等于0时，按照默认值1处理。

建议此环境变量取值不超过当前环境的CPU核数，否则会造成性能劣化。

## 配置示例

```bash
export MAX_COMPILE_CORE_NUMBER=5
```

## 支持的型号

全量芯片支持
